# Docker

Local development services are defined in the repo-root `docker-compose.yml` (Postgres, Redis,
MinIO, Mailpit). This folder is reserved for application Dockerfiles once a deployment target is
chosen — none exist yet, per Bootstrap's scope (no production deployment infrastructure).
