# Terraform

Reserved for production infrastructure-as-code. Not used during local development (see
`docker-compose.yml` at the repo root) or Bootstrap. Populated starting with Phase 20 — Production
Hardening (`docs/09-Implementation/24-CODEX-IMPLEMENTATION-ROADMAP.md`).
