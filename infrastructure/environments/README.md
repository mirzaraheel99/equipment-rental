# Environments

Reserved for environment-specific configuration (staging, production) once a hosting target is
chosen. Local development configuration lives in the repo-root `.env.example` and
`docker-compose.yml` instead.
